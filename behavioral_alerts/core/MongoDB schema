{
  "user_id": String, // Unique UUID (e.g., "ee505198-d6d0-49b3-aa71-af86e421f4be")
  "name": String, // User name (e.g., "Test User")
  "email": String, // Unique email (e.g., "test@example.com")
  "phone": String, // Phone number (e.g., "+33612345678")
  "emergency_contact_phone": String, // Emergency contact (e.g., "+33687654321")
  "created_at": ISODate, // Creation timestamp (e.g., "2025-07-29T11:21:00Z")
  "behavior_profile": { // Optional, only after 10+ location records
    "centroids": [
      {
        "cluster_id": Int32, // Cluster ID (e.g., 0)
        "center": {
          "type": "Point",
          "coordinates": [Double, Double] // [lon, lat] (e.g., [2.3522, 48.8566])
        },
        "radius": Double // Radius in meters (e.g., 500.0)
      }
    ],
    "hour_freq": {String: Double}, // Hour frequencies (e.g., {"0": 0.1, "1": 0.05, ...})
    "weekday_freq": {String: Double}, // Weekday frequencies (e.g., {"0": 0.14, ...})
    "month_freq": {String: Double}, // Month frequencies (e.g., {"1": 0.083, ...})
    "last_updated": ISODate // Profile update timestamp
  },
  "ml_incident_model": { // Optional, serialized model
    "xgboost_model": String, // Base64-encoded XGBClassifier
    "scaler": String, // Base64-encoded StandardScaler
    "saved_at": ISODate // Model save timestamp
  }
}